# [CM-004] Reframe CHANGEMATE.md agent persona: senior technical PM, not senior dev

- **Status**: in-progress
- **Priority**: Medium
- **Effort**: M
- **Assigned to**: claude
- **Started**: 2026-04-14 14:56
- **Completed**: 

## Goal
Rewrite the persona and working-style sections of `CHANGEMATE.md` so the agent operates as a senior technical product manager shaping features inside feature sets — not as a senior developer. Add a reusable `product-manager` skill that owns ticket intake.

## Why
The current opening line ("You are a senior developer and project manager") pushes agents toward implementation voice. The real job at the ticket layer is PM work: crisp problem statements, desired outputs, success/failure criteria, and handoff notes that tell the developer what to watch. Developer tone belongs downstream, inside the build phase.

## Done when
- Opening persona rewritten to senior technical PM voice
- 6-question numbered intake retired; replaced with PM-skill-driven "draft first, ask second" flow
- Ticket file format gains `## Desired output`, `## Success signals`, `## Failure signals`, `## Tests`
- Ticket file format gains `**Feature set**` field
- New `skills/product-manager/SKILL.md` written from scratch (no external clone), wired into `setup.sh`
- `build_lib.py` parses new sections + feature_set; legacy tickets still parse
- `build.sh` renders feature set on card face, new sections on card open
- pytest harness + GitHub Actions CI runs on every PR
- CM-003 and CM-005–CM-009 backfilled into the new format
- All work split into 6 phases (≤5 files each), each with explicit user approval gate

## Notes
**Plan**: see `tasks/todo.md` for the six-phase build.

**Phase 1 in progress**: CHANGEMATE.md persona sweep + new intake flow + new format documented.

**Sequencing**: this lands before CM-003 so relationship fields slot cleanly into the new format.

**Out of scope** (split to other tickets):
- Relationship chips on card face → CM-003
- Second-LLM review of drafts → CM-010
